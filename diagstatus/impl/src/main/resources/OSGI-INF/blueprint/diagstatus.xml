<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

  <!-- We need this hand-written BP XML, in addition to the one generated by the blueprint-maven-plugin,
       due to https://issues.apache.org/jira/browse/ARIES-1740; when that is fixed we could probably ditch
       this (and instead add @OsgiService to the @Inject List<ServiceStatusProvider> in DiagStatusServiceImpl. -->

  <reference-list id="list"
                  member-type="service-object"
                  interface="org.opendaylight.infrautils.diagstatus.ServiceStatusProvider"
                  availability="optional" />

</blueprint>
